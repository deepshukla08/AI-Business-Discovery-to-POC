"""Graph assembly.

    state.py     the shared object every node reads and writes
    pipeline.py  wires app/agents into a StateGraph, with the checkpointer that
                 lets the run pause at gap analysis and resume once a human answers
"""
